[project]
name = "backend"
version = "0.1.0"
description = "FastAPI backend for the weather map app."
readme = "../README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "pandas>=2.2.0",
    "requests>=2.32.0",
    "uvicorn>=0.34.0",
]
